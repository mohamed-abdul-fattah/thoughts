<?php

use App\Entities\Shelf;
use App\ViewHelpers\HomeHelper;

/**
 * @var array $shelves
 * @var Shelf $shelf
 * @var HomeHelper $helper
 */
?>
<!DOCTYPE html>
<html lang="ar" dir="rtl">
<?php $helper->includePartial('layout/head'); ?>
<body>
    <div class="container">
        <?php $helper->includePartial('layout/header') ?>
        <h2 class="t-size-2">أرفف المكتبة</h2>
        <a href="">رف جديد +</a>
        <div class="d-flex">
            <?php $helper->includePartial('shared/booksArt'); ?>
            <table>
                <thead>
                    <tr>
                        <th>عنوان الرف</th>
                        <th>الإجراءات</th>
                    </tr>
                </thead>
                <tbody>
                    <?php foreach ($shelves as $shelf): ?>
                        <tr>
                            <td><?= $helper->getTitle($shelf) ?></td>
                            <td>
                                <a href="/shelves?shelfId=<?= $shelf->getId() ?>">عرض</a>
                                |
                                <a href="">تعديل</a>
                                |
                                <a href="">حذف</a>
                            </td>
                        </tr>
                    <?php endforeach; ?>
                </tbody>
            </table>
        </div>
    </div>
</body>
</html>
